assert()：断言，判断地址是否有效
    参数传的是一个判断表达式
    如: assert(NULL!=arr);
判断arr地址是否为空/是否有效，如果
arr!=NULL为真，就执行后面的代码；
如果arr!=NULL为假，那么该函数结束
函数的运行。

this指针:
    每个类对象的成员函数都有一个this指针
，指针调用对象，哪个对象调用成员函数，this
指针就指向那个指针。